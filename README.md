# Prettier for vscode does not format files in folders starting with a "."

* Clone the repo
* Install node modules
* Try to format the file in `/folder` and `/.folder`, notice that in `/.folder` eslintrc.js is not respected